<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SettingsActivity" >

    <TextView
        android:id="@+id/textViewSettingsUsername"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/textUsername"
        android:textSize="15dp" />

    <EditText
        android:id="@+id/textSettingsUsername"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/textViewSettingsUsername"
        android:layout_below="@+id/textViewSettingsUsername"
        android:layout_marginTop="16dp"
        android:ems="10" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/buttonSettingsSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/textSettingsUsername"
        android:layout_below="@+id/textSettingsUsername"
        android:layout_marginTop="34dp"
        android:onClick="saveUsername"
        android:text="@string/buttonSave" />

</RelativeLayout>